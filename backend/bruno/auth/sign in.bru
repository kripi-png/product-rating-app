meta {
  name: sign in
  type: http
  seq: 2
}

post {
  url: http://localhost:3000/auth/sign_in
  body: json
  auth: none
}

body:json {
  {
    "email": "asd@asd.com",
    "password": "asd123"
  }
}

script:post-response {
  // update env JWT_TOKEN secret automatically
  const token = res?.body?.token;
  if (token)
    bru.setEnvVar("JWT_TOKEN",res.body.token)
}
