meta {
  name: TEST
  type: http
  seq: 3
}

get {
  url: http://localhost:3000/auth/test
  body: none
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

docs {
  This endpoint is for checking if a JWT token is correct and working.
  
  The sign in request automatically updates environment secrets with the token, if credentials were entered correctly.
}
